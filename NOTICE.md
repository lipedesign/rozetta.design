# Third-party notices

Rozetta.design is licensed under **AGPL-3.0** (see [`LICENSE`](./LICENSE)). This
file lists notable third-party code and assets and their licenses. All npm
dependencies retain their own licenses (see each package under `node_modules/`
and the dependency list in `package.json`).

## shadcn/ui

UI primitives under `src/components/ui/` are generated/vendored via
[shadcn/ui](https://ui.shadcn.com) (MIT) and live in-tree by design. Configuration
is in `components.json`.

## Base UI

Component behavior is built on [Base UI](https://base-ui.com) (`@base-ui/react`,
MIT).

## DTCG / W3C Design Tokens

Token files follow the [DTCG / W3C Design Tokens](https://www.designtokens.org/tr/drafts/format/)
format.

---

To add an attribution: vendor the upstream `LICENSE` alongside the copied files
and add a short section here describing what was copied and from where.
